<div class='posts'>
  <% @posts.each do |post| %>
    <%= div_for(post) do %>
      <h2><%= post.title %></h2>
      <p><%= post.content %></p>
    <% end %>
  <% end %>
</div>

<%= link_to('Create New Post', new_post_path) if user_signed_in? %>
